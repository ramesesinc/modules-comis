"entity.(apptype|appno|appyear|dtapplied|dtapproved|dtexpiry)" [ entity.online == 1 || entity.online ] {
    enabled:false;
    required:false;
}

"entity.(apptype|appno|appyear|dtapplied|dtapproved|dtexpiry)" [ entity.online == 0 || !entity.online ] {
    enabled:true;
    required:true;
}
